services:
  router:
    build:
      context: ../
      dockerfile: build/Dockerfile.router
    image: public.ecr.aws/zebrunner/router:$E3S_VERSION
  scaler:
    build:
      context: ../
      dockerfile: build/Dockerfile.scaler
    image: public.ecr.aws/zebrunner/scaler:$E3S_VERSION
  migration:
    build:
      context: ../
      dockerfile: build/Dockerfile.migration
    image: public.ecr.aws/zebrunner/migration:latest
  task-definitions:
    build:
      context: ../
      dockerfile: build/Dockerfile.task-definitions
    image: public.ecr.aws/zebrunner/task-definitions:$E3S_VERSION
